window.LIVE_CONFIG = {
  // Para datos reales, crea una cuenta en https://www.football-data.org/client/register
  // y pega tu token aqui. Ojo: en produccion conviene usar un backend/proxy
  // para no exponer el token en el navegador.
  provider: 'football-data',
  enabled: true,
  token: '',
  competition: 'WC',
  season: 2026,
  proxyBaseUrl: '/api/football-data/v4',
};
